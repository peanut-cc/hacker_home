为了防止个别服务器没打开伪静态支持模块而造成的500服务器内部错误，现将伪静态规则文件，放到根目录外面，如果你的服务器支持伪静态，且你希望实现伪静态，请把.htaccess文件放到更目录，同时设置规则文件的RewriteBase。
为了解析正确，请将.htaccess文件里面的RewriteBase 设置为你的程序目录  。例如，官方的首页访问路径为http://www.bluecms.net/demo，我们则设置RewriteBase /demo;如果就是安装在根目录下，就不要修改了。